# Python Iterators
Iterator is an object with which we can iterate the iterable to get one item at a time from iterable

Any object can be iterator if it implements _\_\_iter\_\__() and _\_\_next\_\__()

Iterable is a sequence
